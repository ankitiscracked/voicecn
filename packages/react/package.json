{
  "name": "@usevoice/react",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "peerDependencies": {
    "react": "^18.3.0"
  },
  "dependencies": {
    "@usevoice/core": "workspace:*"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --dts --clean --format esm --tsconfig tsconfig.build.json",
    "dev": "tsup src/index.ts --dts --watch --format esm --tsconfig tsconfig.build.json",
    "test": "vitest run",
    "lint": "echo \"todo: lint react\"",
    "clean": "rm -rf dist"
  }
}
